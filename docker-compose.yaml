services:
#  web:
#    build:
#      context: ./
#      dockerfile: dockerfile
#    ports:
#      - "8080:8080"
#    depends_on:
#      - db

  postgres:
    container_name: postgres
    image: docker.io/postgres:17
    restart: always
    environment:
      POSTGRES_USER: postgres # change
      POSTGRES_PASSWORD: mysecretpassword # change
      POSTGRES_DB: range_tracker
    ports:
      5432:5432
    volumes:
      - ./data:/var/lib/postgresql/data:Z
